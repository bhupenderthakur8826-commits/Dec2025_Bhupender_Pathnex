# Kubernetes — Deployment with 2 Replicas
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ec2user-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ec2user-app
  template:
    metadata:
      labels:
        app: ec2user-app
    spec:
      containers:
        - name: app
          image: nginx
          ports:
            - containerPort: 80


#### Shell Script — Disk Usage
#!/bin/bash
df -h
